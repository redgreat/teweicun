---
description: 
alwaysApply: true
---

# 项目工作流与输出规范

## 语言与沟通

- 默认使用中文回复。
- 不在项目中额外生成“总结类文档”。

## 目录约定

- 所有脚本文件放在 `scripts/`。
- 所有测试文件放在 `test/`。
- 必要文档放在 `docs/`。

## 注释与文件头

- 不要随意添加注释；仅在函数级或文档级有必要时添加。
- 所有源码文件必须包含文件头，说明：
  - 代码功能
  - 创建时间
  - 创建人

## 数据库新列与 Go 映射

- 可为 NULL 的列：`SELECT` 中对扫入 `string` 的字段用 `COALESCE(..., '')`，或 DTO 使用指针/Null 类型；详见根目录 `CLAUDE.md`「新增可空列与 Go 查询映射」。
- 对数据库的改动都总结一个SQL文件放在sql/migrations文件夹里，不要动sql/schema.sql，这个文件是导出表结构备份用的
- 数据库连接在 configs/config.yaml 里面，每次迭代数据库表结构或者数据时，直接执行就可以了
- 每个迁移脚本头部必须包含 `-- MIGRATION_ID: <唯一ID>` 与 `-- MIGRATION_APPLIED: pending`，用于发布防重。
- 数据库迁移统一通过 `scripts/migrate.sh` 执行；执行成功后会写入 `schema_migration_history`，并把脚本头部 `MIGRATION_APPLIED` 更新为已执行标记，后续不再重复执行。

## 示例

```text
✅ 新增数据库初始化脚本 -> scripts/init_db.sh
❌ 新增数据库初始化脚本 -> tools/init_db.sh
```

```text
✅ 新增接口测试 -> test/api/user_api_test.go
❌ 新增接口测试 -> internal/test/user_api_test.go
```
