---
description: TeWeiCun 项目工作流与 AI 编码约束
alwaysApply: true
---

# 项目工作流与输出规范

## 语言与入口

- 默认使用中文回复。
- 仓库级 AI 规则以根目录 `AGENTS.md` 为准，长期调试记忆见 `MEMORY.md`。
- 不额外生成无用户要求的总结类文档。

## 项目事实

- 架构是 DB-First：核心业务逻辑、事务、状态流转、库存一致性优先放 PostgreSQL。
- 后端是 Go + Gin + pgx/pgxpool，禁止 ORM。
- 前端是 `frontend/` 下的 SvelteKit/Svelte 5，不是 Vue。
- API 统一前缀 `/api/v1`，前端 API client 在 `frontend/src/lib/api/client.ts`。

## 目录约定

- 脚本文件放 `scripts/`。
- 测试文件放 `test/`。
- 必要文档放 `docs/`，AI 工具入口文档放仓库根目录。
- 数据库迁移放 `sql/migrations/`，不要直接改 `sql/schema.sql`。

## 数据库与 Go 映射

- 新增 SQL 迁移沿用当前三位编号格式，例如 `016_fix_xxx.sql`。
- 新增迁移脚本头部必须包含 `-- MIGRATION_ID: <唯一ID>` 与 `-- MIGRATION_APPLIED: pending`，用于 `scripts/migrate.sh` 防重执行。
- 部分历史迁移可能缺少头部标识，非相关任务不要批量重写历史 SQL。
- 数据库对象和新增列需要中文 `COMMENT`。
- 可空列、LEFT JOIN 字段扫入 Go `string` 前，SQL 侧优先 `COALESCE(..., '')`；或使用 `*string`、`sql.NullString`、`pgtype.Text`。
- 调库存、单据确认、退货、领退料时，重点检查存储过程和 `quantity`、`locked_quantity`、`in_transit_quantity`。

## 前端调试

- 使用 Svelte 5 runes 和现有组件。
- 列表页重置筛选并回第一页时，`DataGrid` 页码必须与父组件同步，推荐 `bind:page`。
- 不要引入 Vue/Element Plus 写法。

## 验证命令

```powershell
go run cmd/server/main.go -c configs/config.dev.yaml
make test
make test-flow
make lint
cd frontend; npm run check
cd frontend; npm run lint
```
